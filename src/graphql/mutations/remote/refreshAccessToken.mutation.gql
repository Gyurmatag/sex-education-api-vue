mutation RefreshAccessToken($availableRefreshToken: String!) {
    refreshAccessToken(availableRefreshToken: $availableRefreshToken) {
        accessToken
        accessTokenExpiration
        refreshToken
        refreshTokenExpiration
        user {
            id
            username
            role
        }
    }
}
