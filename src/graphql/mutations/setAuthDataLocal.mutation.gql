mutation SetAuthDataLocal($accessToken: String!, $refreshToken: String!) {
    setAuthDataLocal(accessToken: $accessToken, refreshToken: $refreshToken) @client {
        accessToken
        refreshToken
    }
}
